data:
  registries.conf: |
    registries:
    - name: docker
      prefix: docker.io
      api_url: https://index.docker.io
      ping: yes
      credentials: pullsecret:argocd/repo-docker-token
      defaultns: library
